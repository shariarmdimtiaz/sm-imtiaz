/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
    // reactStrictMode: false,
    // swcMinify: true,
    // output: "export",
  },
};

module.exports = nextConfig;
